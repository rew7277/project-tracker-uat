[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --worker-class gevent --worker-connections 100 --timeout 120 --keep-alive 5 --max-requests 1000 --max-requests-jitter 100 app:app"
